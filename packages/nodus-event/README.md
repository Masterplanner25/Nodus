# `nodus-event`

`nodus-event` is a Python-first event framework for local handlers,
distributed publish adapters, webhook fanout, event causality, and replay.

The public Python API is the canonical contract that future thin Nodus
builtins should wrap.
