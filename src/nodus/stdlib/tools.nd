fn execute(name, args) {
    return tool_call(name, args)
}

fn available() {
    return tool_available()
}

fn has(name) {
    return tool_has(name)
}

fn describe(name) {
    return tool_describe(name)
}
