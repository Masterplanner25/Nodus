# header
import { b, a } from "mod.nd"

// trailing import
// mid
export { z, y }
# trailing export list
export { x } from "other.nd"

// reexport
# before body
// sum
export fn add(a, b) {
    return a + b
}

export let value = 1
// trailing let
