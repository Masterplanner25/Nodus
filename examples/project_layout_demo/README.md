# Project Layout Demo

This example shows a small multi-file Nodus project with:

- relative imports
- `export fn` declarations
- package resolution through `utils/index.nd`
- standard library imports

Run it from the repository root:

```bash
nodus run examples/project_layout_demo/main.nd
```
